{{title "Percy: Login instructions"}}
<FixedTopHeader />

<SsoLoginPanel
  @connectionName={{this.connectionName}}
  @provider={{this.provider}}
  @organizationName={{this.organizationName}}
  @required={{this.required}}
/>

<MainFooter />
