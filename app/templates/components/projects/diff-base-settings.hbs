<div {{action 'setAutomatic'}} role="button" class="ProjectsDiffBaseSettings-setting {{if (eq this.project.diffBase "automatic") "ProjectsDiffBaseSettings-setting--selected"}}">
  {{#if (eq this.project.diffBase "automatic")}}
    <SavingIndicator @isSaving={{this.isSaving}} @isSaveSuccessful={{this.isSaveSuccessful}} />
    {{fa-icon "arrow-right"}}
  {{/if}}
  <span class="ProjectsDiffBaseSettings-title">
    Automatic diff base (default)
  </span>
  <p>
    Automatically use the last <strong>successful</strong> build from the <code>master</code> branch as the baseline for diffs.
  </p>
</div>
<div {{action 'setManual'}} role="button" class="ProjectsDiffBaseSettings-setting {{if (eq this.project.diffBase "manual") "ProjectsDiffBaseSettings-setting--selected"}}">
  {{#if (eq this.project.diffBase "manual")}}
    <SavingIndicator @isSaving={{this.isSaving}} @isSaveSuccessful={{this.isSaveSuccessful}} />
    {{fa-icon "arrow-right"}}
  {{/if}}
  <span class="ProjectsDiffBaseSettings-title">Manual diff base</span>
  <p>
    Use the latest <strong><em style="color: green">{{fa-icon "fa-thumbs-up"}} Approved</em></strong> build from the <code>master</code> branch as the baseline for diffs.<br>
    You must manually approve <code>master</code> builds to change the baseline for future builds.
  </p>
</div>
