<PercyBtn
  @handleClick={{perform this.toggleArchiveState}}
  @isPrimaryButton={{true}}
  class="mt-2"
  @testLabel="project-archive-toggle-button"
>
  {{#if this.project.isEnabled}}
    Archive project
  {{else}}
    Enable Project
  {{/if}}
</PercyBtn>
