{{fa-icon 'clock-o'}}

{{#if this.build.finishedAt}}
  {{#if this.build.durationHours}}{{this.build.durationHours}} h{{/if}}
  {{#if this.build.durationMinutes}}{{this.build.durationMinutes}} min{{/if}}
  {{#if this.build.durationSeconds}}{{this.build.durationSeconds}} sec{{/if}}
{{else}}
  --
{{/if}}
