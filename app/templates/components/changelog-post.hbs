<article id={{this.postId}} class="changelog-post sm:flex mb-4">
  <div
    class="changelog-post-detail flex relative flex-col items-start sm:items-end mr-4 pt-3 {{if (eq this.tag "feature") 'is-unreviewed'}} {{if (eq this.tag "integration") 'is-unchanged'}} {{if (eq this.tag "update") 'is-approved'}} {{if (eq this.tag "bugfix") 'is-failed'}} {{if (eq this.tag "sdk") 'is-changes-requested'}}"
    style="min-width: 100px;">
    <div class="text-secondary">{{this.date}}</div>
    <div class="changelog-post-detail-tag font-mono font-normal tracking-sm text-sm rounded-full text-white mb-1 px-1">{{this.tag}}</div>
  </div>
  <div class="flex-1 bg-white rounded shadow-1 pt-3 px-2 pb-1">
    {{yield}}
  </div>
</article>
