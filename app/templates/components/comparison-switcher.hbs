<div class="btn-group btn-group-alt" data-test-comparison-switcher>
  {{#each (sort-by "width" comparisons) as |comparison|}}
    {{comparison-switcher-item
      selectedWidth=selectedWidth
      width=comparison.width
      comparisons=comparisons
      updateSelectedWidth=updateSelectedWidth
      data-test-ComparisonSwitcher-button=true
    }}
  {{/each}}
</div>
