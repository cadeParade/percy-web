{{title (concat project.name " dashboard | Percy") replace=true}}

{{did-insert fire=(action "updateIntercom")}}

{{fixed-top-header currentOrganization=project.organization project=project}}

<div data-test-project-page>
  {{project-container
    project=project
    selectedBranch='All branches'
    infinityBuilds=infinityBuilds
    isUserMember=isUserMember
  }}
</div>

