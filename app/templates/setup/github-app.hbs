{{title "Completing GitHub App Setup"}}
<FixedTopHeader />
  <SetupGithubApp
    @installationId={{this.installationId}}
    @afterAppInstalled={{route-action "afterAppInstalled"}}
  />
<MainFooter />
