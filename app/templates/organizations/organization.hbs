<FixedTopHeader @currentOrganization={{this.model}} />
{{#if this.shouldShowNav}}
  <DashboardNavWrapper
    @organization={{this.model}}
    @showMemberLinks={{this.isUserMember}}
  >
    {{outlet}}
  </DashboardNavWrapper>
{{else}}
  {{outlet}}
{{/if}}
