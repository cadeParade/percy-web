{{title (concat model.name " billing | Percy") replace=true}}

{{#organizations/settings-nav-wrapper organization=model}}
  {{organizations/billing-section
    organization=model
    currentUsageStats=currentUsageStats
  }}
{{/organizations/settings-nav-wrapper}}
