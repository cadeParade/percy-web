{{title (concat this.model.name " billing | Percy") replace=true}}

<DashboardNavWrapper
  @organization={{this.organization}}
  @showMemberLinks={{true}}
>
  <Organizations::BillingSection
    @organization={{this.organization}}
    @currentUsageStats={{this.currentUsageStats}}
    @updateEmail={{route-action "updateEmail"}}
    @updateSubscription={{route-action "updateSubscription"}}
    @updateCreditCard={{route-action "updateCreditCard"}}
  />
</DashboardNavWrapper>
