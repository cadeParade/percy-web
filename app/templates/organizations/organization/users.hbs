{{title (concat this.organization.name " users | Percy") replace=true}}

<DashboardNavWrapper
  @organization={{this.organization}}
  @showMemberLinks={{true}}
>
  {{outlet}}
  <Organizations::ManageUsersList
    @organization={{this.organization}}
    @organizationUsers={{this.organizationUsers}}
  />
</DashboardNavWrapper>
