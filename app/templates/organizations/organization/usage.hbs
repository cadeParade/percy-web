{{title (concat model.name " Usage | Percy") replace=true}}

{{#dashboard-nav-wrapper organization=organization showMemberLinks=true}}
  {{organizations/usage-section
    organization=organization
    currentUsageStats=currentUsageStats
    usageNotificationSetting=usageNotificationSetting
  }}
{{/dashboard-nav-wrapper}}
