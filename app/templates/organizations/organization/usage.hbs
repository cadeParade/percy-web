{{title (concat this.model.name " Usage | Percy") replace=true}}

<DashboardNavWrapper
  @organization={{this.organization}}
  @showMemberLinks={{true}}
>
  <Organizations::UsageSection
    @organization={{this.organization}}
    @currentUsageStats={{this.currentUsageStats}}
    @usageNotificationSetting={{this.usageNotificationSetting}}
  />
</DashboardNavWrapper>
